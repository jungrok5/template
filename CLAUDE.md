# CLAUDE.md — 공통 작업 규칙

> **시제: 지금.** 여기 적힌 것은 **지금 그렇다.** 앞으로 할 일도, 그때 그랬던 것도 아니다 (§2.8).

저장소 루트의 공통 규칙. **모든 작업에 적용된다.** 하위 폴더에 더 구체적인 규칙이
있으면 그쪽이 우선하되, 이 문서와 충돌하면 **이 문서를 고친 뒤** 진행한다.
프로젝트 무관 베이스라 `<!-- PROJECT -->` 로 표시된 곳만 채우면 된다 — 방법은
[`README.md`](./README.md).

**이 파일은 규칙만 담는다.** 왜 그 규칙이 생겼는지 · 안 지키면 무엇이 무너지는지는
[`docs/rules-rationale.md`](./docs/rules-rationale.md) 에 있다 — 여기 사연을 쌓으면
**매 세션 읽는 비용**이 된다.

---

## 0. 이 규칙들의 전제

**목표는 개발 공정을 가능한 한 많이 AI 가 수행하게 하는 것이다** — 설계 · 구현 ·
테스트 · 배포 · 운영까지. 사람 팀이라면 회의와 구두 합의로 넘어가는 것들이 전부
무너진다. 그래서 **규칙 하나를 추가할 때 물어볼 것은 하나다: 「이것을 사람의
주의력이 아니라 도구가 강제할 수 있는가.」** 강제할 수 있으면 관문(§4.2)으로 만들고,
없으면 왜 못 하는지를 적는다.

⚠️ **이 문서를 그대로 다 지키는 것이 목적이 아니다.** 규칙마다 비용이 있다. 규모에
안 맞는 규칙은 **삭제하고 왜 뺐는지 한 줄 남긴다.** 조용히 어기지 않는다.

### 0.1 사람에게 보고하는 법 ← **가장 자주 어긴다**

**상대가 알아야 하는 것은 빠짐없이, 나만 아는 것은 뺀다.** 짧게 쓰라는 규칙이
아니다 — 문제는 길이가 아니라 방향이다. **판정은 하나다: 「이걸 모르면 상대가 잘못
판단하는가.」** 그렇다면 말한다, 길어져도. 아니면 문서와 커밋으로 보낸다.

| 순서 | |
|---|---|
| 1. 결론 | 한 줄 |
| 2. **왜** | **한 줄.** 고를 것이 있었으면 반드시 — 「A 말고 B 로 했다, 왜냐면」 |
| 3. 달라진 것 | **상대가 보는 것 기준**(화면 · 동작 · 값) |
| 4. 할 일 | 사람이 직접 해야 하는 것. 없으면 **「없음」** |
| 5. 알아 둘 것 | **나중에 놀랄 것** — 못 한 것 · 부작용 · 아직 확인 안 된 것 |

**반드시 말한다** (짧게 못 하겠으면 길게라도) — 요청과 **다르게** 한 것과 그 이유 ·
**못 한 것 · 안 되는 것**과 그것이 무엇을 막는지 · **되돌리기 어려운 결정**(지운 것 ·
밖으로 나간 것 · 돈이 드는 것) · **아직 확인 안 된 것**(「코드는 됐고 실기기는 안 봤다」)

**뺀다** — 이미 한 말 다시 하기 · 과정 중계(무엇을 읽었고 어디를 고쳤고) · 내부
이름(**사람이 직접 다룰 때만** 쓴다) · 검증 숫자(「통과」 한 줄이면 된다. 숫자는
커밋에). 「무엇을 적었고 · 어디로 옮겼고 · 몇 개 통과했는지」는 **내 작업 일지**다.

⚠️ **관문이 못 잡는다.** 지적받을 때마다 이 절을 다시 읽는다.

---

## 1. 작업 시작 전: 문서를 먼저 읽는다 (필수)

| 위치 | 내용 |
|---|---|
| [`GLOSSARY.md`](./GLOSSARY.md) | **용어집 — 모든 작업의 전제** (§2.1) |
| [`docs/spec/`](./docs/spec/README.md) | **명세 — 「무엇을·왜」의 단일 진실 원천** (§2.4) |
| `docs/` | 설계 문서 — 「어떻게 · 어떤 함정이 있었나」 |
| [`docs/docs-vs-code.json`](./docs/docs-vs-code.json) | **지운 이름 · 지운 말 · 문서의 시제** 등록부 (§2.5 · §2.8) |
| <!-- PROJECT --> | 컴포넌트별 문서 위치를 여기에 추가한다 |

**경계에 걸친 것은 한쪽만 고치면 반대쪽이 조용히 깨진다.** 프로토콜 · 스키마 · API
는 **어느 쪽이 권위인지 한 곳에 못 박고**, 반대쪽 문서는 그것의 관점별 매핑으로
취급한다. 바꿀 때는 **권위 문서를 먼저** 고친다.

> <!-- PROJECT --> 권위 문서: `docs/…` (예: 서버가 권위면 서버 프로토콜 문서)

**시작하기 전에 답한다.** 관련 문서를 읽었는가(걸쳐 있으면 양쪽 다) · 쓰려는 용어가
용어집에 있는가(§2.1) · 명세를 바꾸는 작업인가(§2.4) · 개념을 지우는 작업인가(§2.5) ·
계획 문서의 어느 항목인가(§3) · 성공 기준이 무엇인가(§5.4).

---

## 2. 문서 규칙

### 2.1 용어는 용어집에서 먼저 찾는다 (필수)

**없으면 등록한 뒤 작업한다. 새 이름을 즉흥적으로 만들지 않는다.** 문서 · 코드 ·
커밋 메시지 · 명세 전부에 적용된다. 특히 **프레임워크 · 업계 관례 · 우리 도메인이
같은 단어를 다른 뜻으로 쓰는 경우**가 위험하다 — 그 충돌과 **기존 코드가 다른 이름을
쓸 때** 하는 일은 [`GLOSSARY.md`](./GLOSSARY.md) §1 에 있다.

### 2.2 위치와 분담

폴더는 §6 에 있다. 나누는 기준은 하나다 — **「무엇을 · 왜」는 명세, 「어떻게 · 어떤
함정이 있었나」는 설계 문서.** 수치와 규칙은 명세(「무활동 30분이면 세션 만료」),
구조와 함정은 설계 문서(「TTL 계산은 올림이다 — 내림하면 경계에서 1초 짧아진다」).
⚠️ **같은 내용을 양쪽에 쓰지 않는다** (§2.7).

⚠️ **명세를 특정 컴포넌트 밑에 두지 않는다.** 명세는 모든 컴포넌트의 상위 문서다 —
한쪽 밑에 두면 그쪽의 구현 문서로 읽히고 반대쪽이 안 읽는다.

### 2.3 포맷 — `.md` 가 기본이다

AI 는 원본을 읽고(토큰 효율 · grep · diff) 사람은 렌더된 것을 본다. 다이어그램은
```` ```mermaid ```` 펜스로. `.html` 은 커스텀 SVG · 레이아웃이 꼭 필요하거나 문서
자체가 발표용 산출물일 때만 쓴다. **같은 내용을 두 포맷으로 중복 유지하지 않는다.**
⚠️ **mermaid 문법 오류는 빌드를 실패시키지 않고 빈 상자로 렌더된다** — 별도 관문이
필요하다 (§4.2).

### 2.4 명세와 코드는 서로를 가리킨다 (필수)

**「이 시스템이 무엇을 어떻게 하는가」의 단일 진실 원천은
[`docs/spec/`](./docs/spec/README.md) 이다. 코드를 먼저 고치고 명세를 나중에 쓰지
않는다 — 나중은 오지 않는다.** 규칙을 바꿀 때도 새로 만들 때도 **① 명세 → ② 코드 ·
태그 → ③ 검증 테스트** 순이다.

규칙마다 `SPEC-<영역>-<번호>` ID 를 두고 **구현 파일과 검증 테스트를 명시**하며,
코드가 같은 ID 를 주석으로 되가리킨다. **형식 · 필드와 검사기가 잡는 것은
[`docs/spec/README.md`](./docs/spec/README.md) 에 있다** — 여기 다시 적지 않는다 (§2.7).

### 2.5 개념을 지우면 **말도 같이 지운다** (필수)

⚠️ **개념보다 어휘가 오래 산다.** 셋을 함께 한다 — **하나라도 빠지면 지운 것이 아니다.**

1. **코드에서 이름을 지운다** — 죽은 함수를 테스트만 부르고 있으면 그 테스트도
2. **[`GLOSSARY.md`](./GLOSSARY.md) 금지어 표에 한 줄** — 무엇 대신 무엇을 쓰는지와 **왜 지웠는지**
3. **[`docs/docs-vs-code.json`](./docs/docs-vs-code.json) 에 등록** — 여기가 이 규칙을
   **도구가 강제하게** 만드는 자리다 (§0). 필드와 예시는 그 파일 안에 있다 (§2.7)

**코드**에서는 지운 이름이 식별자로 다시 나타나는 것을 막고, **글**에서는 지운 말을
**묘비 없이는** 못 쓰게 한다. 묘비(「한때」·「지웠」·「사라졌」…)가 같은 문단에 있으면
통과한다 — **겪은 것을 적는 것은 막지 않고 되살아나는 것만 막는다.**

⚠️ **표는 한 줄이 한 문장이다.** 옆줄의 묘비를 빌려 오지 못한다 — 표에서는 같은 줄에
취소선(`~~…~~`)을 쓴다.

### 2.6 문서가 가리키는 이름은 **실재해야 한다**

**「지금」인 문서(§2.8)의 백틱 이름 전부**를 코드와 대조한다. 묘비가 붙은 줄과 코드
펜스 안은 넘어간다 — 기록과 예시는 지시가 아니다.

| 걸렸을 때 | 하는 일 |
|---|---|
| 이름이 바뀐 것 | **문서를 고친다** |
| **아직 안 만든 것** | 문서에 **안 만들었다고 적고**, 등록부에 이유와 함께 등록 |
| 남의 것(외부 라이브러리 · 에셋 팩) | 등록부에 이유와 함께 등록 |

⚠️ **이유 없이 등록하기 시작하면 이 관문은 죽는다.** 등록부의 그 칸은 「왜 코드에
없어도 되는가」를 답하는 자리이지 예외 목록이 아니다.

### 2.7 코드의 값을 문서에 **베끼지 않는다**

⚠️ **같은 목록을 두 곳에 두지 않는다** — 두면 반드시 한쪽이 낡는다. 순서대로 시도한다.

1. **문서가 그 목록을 안 들게 만든다.** 「전부를 한 장으로 모은 것 하나」를 가리키게
   한다 — 가장 확실하다. 낡을 것이 없다.
2. 안 되면 **빠진 것을 잡는 검사를 쓴다.** 문서가 코드보다 **적게** 적은 것이 낡은
   것이다. 문서에만 있는 줄(설명 · 묶음 제목)까지 잡으면 표를 사람이 읽을 수 있게
   쓰지 못한다.
3. 둘 다 안 되면 **왜 못 하는지를 적는다** (§0).

⚠️ **실측값은 어디서 쟀는지가 값의 일부다.** 개발 컨테이너와 실기기는 열 배 차이가
날 수 있다 — **무엇으로 쟀는지**를 값 옆에 적어 다음 세션이 다시 재게 한다.

### 2.8 문서마다 **시제를 머리에 적는다**

문서 머리에 한 줄(`> **시제: 지금.** …`), **같은 값을 등록부에도 적는다** — 관문이
둘을 대조한다.

| 시제 | 무엇 | 없는 이름이 |
|---|---|---|
| **지금** | 지금 그렇다 | **있으면 안 된다** (§2.6) |
| **그때** | 그때 겪은 것의 기록 | 정상이다 |
| **앞으로** | 아직 없는 설계 | 정상이다 |

새 `.md` 를 만들면 관문이 거기서 운다 — 그때 시제를 정하거나, 없어도 되는 문서라면
**이유와 함께** 면제 목록에 넣는다.

---

## 3. 계획 문서는 정리가 아니라 **기준**이다

- **작업 전**: 문서와 다르게 만들 이유가 있으면 **먼저 문서를 고친다.**
- **작업 중**: 문서에 없는 결정을 내렸으면 기록한다. 사라지면 다음 세션이 같은 고민을
  반복하고 다르게 결론 낸다.
- **작업 후**: 마일스톤 상태 · 실제 경로 · 바뀐 스키마 · 폐기한 설계를 반영한다.

**코드와 문서가 어긋나면 둘 중 하나는 버그다.** 어긋난 상태로 두지 않는다.
⚠️ **「확인했다」고 쓰려면 무엇을 어떻게 봤는지 같이 쓴다.** 근거 없이 남으면 다음
세션이 그것을 사실로 믿는다.

---

## 4. 테스트 규칙

### 4.1 기능을 추가하면 테스트도 함께 쓴다

예외 없다. **기능 코드와 테스트 코드는 같은 커밋에 들어간다.**

### 4.2 빌드 게이트

**빌드 시 모든 테스트가 통과해야 한다.** 하나라도 실패하면 병합하지 않는다.
**「로컬에선 됐다」는 근거가 아니다 — CI 가 판정한다.** ⚠️ **관문 전체는 커밋 직전에
한 번이다.** 작업 중에는 **고친 것에 해당하는 검사만** 좁게 돌리고, 무거운
검증(브라우저 · 에셋 굽기 · 증거 수집)은 다 만든 뒤 한 번.

| 관문 | 도구 | 잡는 것 |
|---|---|---|
| 테스트 · 린트 · 타입 검사 | <!-- PROJECT --> | 기능 회귀 · 스타일 드리프트 · 계약 위반 |
| **명세 ↔ 코드 링크** | `tools/spec/check_spec_links.py` | **명세와 구현이 갈라진 것** (§2.4) |
| **문서 ↔ 코드** | `tools/docs/check_docs_vs_code.py` | **되살아난 말 · 없는 이름 · 빠진 시제 표시** (§2.5~§2.8) |
| **베낀 목록** | <!-- PROJECT --> | 문서가 코드보다 **적게** 적은 목록 (§2.7) |
| mermaid 문법 | <!-- PROJECT --> | 빈 상자로 렌더되는 다이어그램 (§2.3) |
| 생성물 미커밋 | `git diff --exit-code` | 손으로 고친 생성 코드 |
| **테스트 증거 규약** | <!-- PROJECT --> | 판정 없는 산출물 · 유출 위험 캡처 (§4.4) |
| **미룬 작업의 트리거** | <!-- PROJECT --> | 트리거가 발화했는데 아무도 모르는 것 (§4.5) |

> **프로젝트 고유 관문을 여기에 계속 추가한다.** 사람이 리뷰에서 반복해 지적하는
> 것이 있으면 그것은 관문 후보다 (§0).

### 4.3 회귀: 한 번 겪은 문제는 다시 겪지 않는다

**재현 테스트를 먼저 쓰고 실패를 확인 → 원인을 고치고 통과를 확인 → 전체를 돌려 다른
것이 안 깨졌는지 확인.** 순서를 바꾸지 않는다. 테스트를 못 쓰겠으면(타이밍 · 환경
의존) **왜 못 쓰는지와 어떻게 감지할지**를 설계 문서의 「함정」 절에 남긴다.

### 4.4 테스트는 증거를 남긴다

화면 · 외부 시스템처럼 자동 단언만으로 확신할 수 없는 것은 산출물을 남긴다.
「돌려 봤다」가 아니라 **판정 · 수치 · 프레임**이 파일로 남아야 완료다.

```
.evidence/<branch>/<test-id>/
├── result.json      # 판정 · 지표 · 커밋 SHA · 실행 시각
├── shot-01.png      # 커밋한다 (회귀 비교용)
└── video.url        # 외부 스토리지 URL — 영상 자체는 커밋하지 않는다
```

**셋은 예외 없다.** ① **판정은 종료 코드로 나간다** — 로그를 사람이 읽어야만 아는
실패는 CI 가 못 잡는다. ② ⚠️ **캡처는 대상 화면만** — 전체 데스크톱 캡처는 금지한다.
③ **영상 · 대용량 바이너리는 저장소에 넣지 않는다** — 외부에 올리고 URL 만 남긴다.

### 4.5 미룬 작업은 트리거로 등록한다

**「지금 하지 않는다」고 정한 것은 [`docs/triggers.json`](./docs/triggers.json) 에
조건과 함께 등록한다.** 검사기가 커밋된 지표로 평가하고 **넘었는데 상태가 `대기` 면
실패한다.** 상태를 `발화` 로 바꾸면 통과한다 — 대신 그때부터 문서에 남는다.
⚠️ 지표가 없어 평가할 수 없으면 `측정불가` 로 두고 **왜 없는지를 적는다**.

### 4.6 「언젠가 멈추는」 구조는 정적으로 검사한다

**런타임에만 드러나고 재현이 어려운 실패는 테스트로 못 잡는다.** 전형적인 후보:
**차단 호출만으로 이루어진 순환**(액터 · 락 순서) · **계층 위반** · **식별자
규약**(ID 중복 · 폐기 재사용).

⚠️ **「금지」가 아니라 「등록하라」로 만든다** — 의도적 예외가 반드시 생긴다. 허용
목록은 파일로 두고 **하나하나에 이유를 적는다.** ⚠️ **허용 단위는 가장 작은
단위(간선 · 심볼)로** — 「이 모듈 쌍은 허용」으로 등록하면 그 사이에 위험한 간선이
하나 더 생겨도 통과한다.

---

## 5. 코딩 원칙

> 출처: [andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md).
> 속도보다 신중함에 치우쳐 있다 — 사소한 작업에는 판단해서 적용한다.

**5.1 코딩 전에 생각한다** — 추측하지 않고 혼란을 감추지 않는다. 가정을 명시하고
불확실하면 묻는다. 해석이 여럿이면 조용히 하나를 고르지 말고 모두 제시한다. 더 단순한
방법이 있으면 말하고, 필요하면 반대 의견을 낸다.

**5.2 단순함 우선** — 문제를 푸는 최소한의 코드. 요청받지 않은 기능 · 유연성 · 설정을
넣지 않고, 한 번 쓰는 코드에 추상화를 만들지 않고, 일어날 수 없는 상황에 예외 처리를
넣지 않는다. **「시니어 엔지니어가 과하다고 할까?」** 그렇다면 단순화한다.

**5.3 최소 범위 수정** — 옆에 있는 코드 · 주석 · 서식을 "개선"하지 않고, 깨지지 않은
것을 리팩터링하지 않는다. 자기 방식이 더 낫다고 생각해도 **기존 스타일에 맞춘다.**
관련 없는 죽은 코드는 **말만 한다.** 단, **내 변경 때문에** 안 쓰이게 된 import ·
변수 · 함수는 제거한다. 판정 기준: **바뀐 모든 줄이 요청으로 직접 추적되는가.**

**5.4 목표 기반 실행** — 작업을 검증 가능한 목표로 바꾸고 검증될 때까지 반복한다.
「검증 추가」 → 「잘못된 입력에 대한 테스트를 쓰고 통과시킨다」. 여러 단계면
`단계 → 검증 방법`을 짧게 밝힌다.

**5.5 주석은 왜를 쓴다** — 무엇을 하는지가 아니라 **왜 그렇게 했는지**, 특히 **하지
않기로 한 것과 그 이유**. 코드는 선택의 결과만 남기고 선택지는 지운다.
⚠️ **이것은 코드와 문서의 규칙이다. 사람에게 하는 보고로 옮기지 않는다** (§0.1).

---

## 6. 폴더 규칙

| 폴더 | 담는 것 |
|---|---|
| `docs/spec/` | **명세** — 「무엇을·왜」의 단일 진실 원천 (§2.4). 코드가 아니다 |
| `docs/` | 설계 문서 — 「어떻게 · 함정」 · 관문이 읽는 등록부 (§2.5 · §4.5) |
| `tools/` | 저장소 전체에 걸치는 도구 · 검사기. 제품 빌드에 포함되지 않는다 |
| `.github/` | CI 워크플로 |
| <!-- PROJECT --> | 컴포넌트 폴더를 여기에 추가한다 |

**제품 코드는 반드시 컴포넌트 폴더 안에 둔다. 루트에 코드를 두지 않는다.** 루트에
두는 문서는 저장소 전체에 걸치는 것뿐이다.

---

## 7. 프로젝트 고유 원칙

<!-- PROJECT -->

> 설계 문서에서 확립된 원칙을 여기에 요약하고 상세는 링크한다. 각 항목은 **「무엇을
> 금지하고 무엇을 기본값으로 삼는가」**로 쓴다 — 취향이 아니라 판정 기준이어야 한다.
>
> 예시(지우고 다시 쓴다): **신뢰 경계**(어느 쪽이 권위인가 · 무엇을 신뢰하지 않는가) ·
> **계약은 인터페이스 뒤에 숨긴다**(상위 코드가 와이어 포맷 · SQL 을 직접 만들지
> 않는다) · **성능 제약은 설계 전제다**(「만들고 나서 최적화」가 아니라 「제약 안에서
> 설계」) · **종류를 만들지 않는다**(「이건 A 종류」 같은 칸을 만들면 채우는 사람이
> 생기고, 그때부터 같은 것을 두 군데서 말하게 된다. 판정은 값에서 나온다) ·
> **언어 · 프레임워크 코딩 표준**(어느 표준을 따르는가. 링크).
