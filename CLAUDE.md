# CLAUDE.md — 공통 작업 규칙

저장소 루트의 공통 규칙. **모든 작업에 적용된다.**
하위 폴더에 더 구체적인 규칙이 있으면 그쪽이 우선하되, 이 문서와 충돌하면 **이 문서를 고친 뒤** 진행한다.

> 이 파일은 프로젝트 무관 베이스다. `<!-- PROJECT -->` 로 표시된 곳만 프로젝트에 맞게 채우면 된다.
> 채우는 방법은 [`README.md`](./README.md) 참조.

---

## 0. 이 규칙들이 왜 이렇게 촘촘한가

**목표는 개발 공정을 가능한 한 많이 AI 가 수행하게 하는 것이다 — 설계 · 구현 · 테스트 · 배포 · 운영까지.**

그것이 아래 모든 규칙의 이유다. 사람 팀이라면 회의 · 구두 합의 · "그때 그렇게 하기로 했잖아"로
넘어가는 것들이, AI 가 공정을 수행하면 전부 무너진다.

| 왜 | 그래서 |
|---|---|
| **AI 는 세션 사이에 기억이 없다** | 결정을 문서에 남기지 않으면 다음 세션이 같은 고민을 다시 하고, 다르게 결론 낸다 (§3) |
| **AI 는 "대충 맞는" 답을 자신 있게 낸다** | "돌아간다"를 완료로 치면 안 된다. 검증 수단이 없으면 완료가 아니다 (§4) |
| **AI 는 문서를 읽지 않으면 코드만 보고 판단한다** | 코드가 곧 명세가 되어 버린다. 그래서 명세와 코드가 서로를 가리키고 CI 가 검사한다 (§2.4) |
| **AI 는 같은 개념에 매번 다른 이름을 붙인다** | 용어집이 강제 규칙이다 (§2.1) |
| **사람이 리뷰로 잡아 주지 못한다** | 판정을 CI 관문으로 옮긴다 (§4.2) |

**규칙 하나를 추가할 때 물어볼 것: "이것을 사람의 주의력이 아니라 도구가 강제할 수 있는가."**
강제할 수 있으면 관문으로 만들고, 없으면 왜 못 하는지를 적는다.

⚠️ **이 문서를 그대로 다 지키는 것이 목적이 아니다.** 규칙마다 비용이 있다.
프로젝트 규모에 안 맞는 규칙은 **삭제하고 왜 뺐는지 한 줄 남긴다.** 조용히 어기지 않는다.

---

## 1. 작업 시작 전: 문서를 먼저 읽는다 (필수)

**작업 영역이 어디든, 관련된 문서를 모두 읽고 시작한다.**

| 위치 | 내용 |
|---|---|
| [`GLOSSARY.md`](./GLOSSARY.md) | **용어집 — 모든 작업의 전제** (§2.1) |
| [`docs/spec/`](./docs/spec/README.md) | **명세 — "무엇을 · 왜"의 단일 진실 원천** (§2.4) |
| `docs/` | 설계 문서 — "어떻게 · 어떤 함정이 있었나" |
| <!-- PROJECT --> | 컴포넌트별 문서 위치를 여기에 추가한다 |

**한쪽만 읽고 작업하면 안 된다.** 인터페이스 · 데이터 모델 · 상태 동기화처럼
**둘 이상의 컴포넌트에 걸친 것**은 한쪽만 보고 고치면 반대쪽이 조용히 깨진다.

경계에 걸친 계약(프로토콜 · 스키마 · API)은 **어느 쪽이 권위인지 한 곳에 못 박고**,
반대쪽 문서는 그것의 관점별 매핑으로 취급한다. 바꿀 때는 **권위 문서를 먼저** 고친다.

> <!-- PROJECT --> 권위 문서: `docs/…` (예: 서버가 권위면 서버 프로토콜 문서)

### 작업 전 체크리스트

1. 관련 문서를 읽었는가 (걸쳐 있으면 양쪽 다)
2. **쓰려는 용어가 [`GLOSSARY.md`](./GLOSSARY.md)에 있는가** — 없으면 등록하고 시작한다 (§2.1)
3. **명세를 바꾸는 작업인가** — 그렇다면 [`docs/spec/`](./docs/spec/README.md)을 먼저 고친다 (§2.4)
4. 이 작업이 계획 문서의 어느 항목인가 — 계획에 없으면 §3의 규칙을 따른다
5. 성공 기준이 무엇인가 (§5.4)

---

## 2. 문서 규칙

### 2.1 용어는 용어집에서 먼저 찾는다 (필수)

**모든 용어는 [`GLOSSARY.md`](./GLOSSARY.md)에서 먼저 찾는다. 없으면 등록한 뒤 작업한다.**

문서 · 코드 · 커밋 메시지 · 명세 전부에 적용된다. **새 이름을 즉흥적으로 만들지 않는다.**

이 규칙이 있는 이유: 같은 개념을 컴포넌트마다 다른 단어로 부르면 **계약과 스키마가 조용히 어긋난다.**
사용자 · 계정 · 프로필 · 멤버를 각자 편한 대로 쓰면
어느 것이 로그인 단위이고 어느 것이 과금 단위인지 코드에서 구분이 사라진다.

특히 위험한 것은 **프레임워크 · 업계 관례 · 우리 도메인이 같은 단어를 다른 뜻으로 쓰는 경우**다.
이런 충돌은 `GLOSSARY.md` §1 에 규칙으로 못 박아 둔다.

기존 코드가 용어집과 다른 이름을 쓰고 있으면 **용어집 §1 에 충돌로 기록**하고 리네임 여부를 결정한다.
어긋난 상태로 두지 않는다 (§3).

### 2.2 위치

- **명세(무엇을 · 왜)는 `docs/spec/`** 에 둔다 (§2.4).
- **설계 문서(어떻게)는 해당 컴포넌트 폴더 안** 에 둔다. 저장소 루트에 흩뿌리지 않는다.
- **루트에 두는 것은 저장소 전체에 걸치는 것뿐이다** — `CLAUDE.md` · `GLOSSARY.md` · `README.md` · `docs/`.

⚠️ **명세가 특정 컴포넌트 밑에 있으면 안 되는 이유**: 명세는 모든 컴포넌트의 상위 문서다.
어느 한쪽 밑에 두면 그쪽의 구현 문서로 읽히고, 반대쪽이 안 읽는다.

### 2.3 포맷 — `.md`가 기본이다

**모든 문서는 `.md`로 쓴다.** AI 는 원본을 읽고(토큰 효율 · grep · diff 추적),
사람은 렌더된 것을 본다. 다이어그램은 ```` ```mermaid ```` 펜스로 넣는다.

| 포맷 | 쓰는 경우 |
|---|---|
| **`.md`** (기본) | 거의 전부 |
| **`.html`** (예외) | 커스텀 SVG · 레이아웃이 꼭 필요할 때, 또는 문서 자체가 발표 · 공유용 산출물일 때 |

**같은 내용을 두 포맷으로 중복 유지하지 않는다.**
`.html` 은 grep · 부분 읽기가 어렵고 검색 · 네비게이션에서 이류 시민이 된다. 고를 때는 이유가 있어야 한다.

⚠️ **mermaid 문법 오류는 빌드를 실패시키지 않고 빈 상자로 렌더된다.** 그래서 별도 관문이 필요하다 (§4.2).

### 2.4 명세와 코드는 서로를 가리킨다 (필수)

**"이 시스템이 무엇을 어떻게 하는가"의 단일 진실 원천은 [`docs/spec/`](./docs/spec/README.md)이다.**

명세 없이 구현이 먼저 가면 **구현이 곧 명세**가 되고, 결정들이 코드 주석과 흩어진 문서에 남는다.
그 상태에서는 "이게 의도인지 버그인지"를 아무도 판정할 수 없다.

#### 순서 — 명세가 먼저다

```
규칙을 바꾼다  →  ① docs/spec/ 을 고친다  →  ② 코드를 맞춘다  →  ③ 테스트를 맞춘다
새 규칙을 만든다 →  ① 새 ID 로 항목을 쓴다  →  ② 구현 + 태그   →  ③ 검증 테스트
```

**코드를 먼저 고치고 명세를 나중에 쓰지 않는다. 나중은 오지 않는다.**

#### 양방향 링크 — 한쪽만 바뀌면 CI 가 실패한다

명세의 각 규칙은 **`SPEC-<영역>-<번호>`** ID 를 갖고, 구현 파일과 검증 테스트를 명시한다.

```markdown
### SPEC-AUTH-003 · 세션 만료

규칙 본문…

- **상태** — ✅
- **구현** — `src/auth/session.ts`
- **검증** — `expires_session_after_idle_timeout`
```

코드는 같은 ID 를 주석으로 되가리킨다.

```ts
// 명세: [SPEC-AUTH-003] 세션 만료
export class SessionPolicy { … }
```

```bash
python3 tools/spec/check_spec_links.py     # CI 가 돌린다
```

| 어긴 경우 | 검사가 잡는 것 |
|---|---|
| 코드만 지웠다 | 명세가 가리키는 파일에 태그가 없다 |
| 명세만 지웠다 | 코드에 명세 없는 태그가 있다 |
| 파일을 옮겼다 | 명세의 경로가 존재하지 않는다 |
| 테스트를 지웠다 | 명세의 검증 이름이 없다 |
| ✅ 인데 검증이 없다 | "돌아간다"를 완료로 적었다 |

**ID 는 재사용하지 않는다** — 폐기하면 [`docs/spec/retired-ids.json`](./docs/spec/retired-ids.json) 으로 옮긴다.
이슈 · 회의록에 남은 참조가 조용히 **다른 규칙**을 가리키면 안 된다.

#### 무엇을 어디에 쓰는가

| | `docs/spec/` | 설계 문서 |
|---|---|---|
| 답하는 질문 | **무엇을 · 왜** | **어떻게 · 어떤 함정이 있었나** |
| 예 | "무활동 30분이면 세션 만료" | "TTL 계산은 올림이다. 내림하면 경계에서 1초 짧아진다" |

**같은 내용을 양쪽에 쓰지 않는다.** 수치와 규칙은 명세, 구조와 함정은 설계 문서다.

---

## 3. 계획 문서는 정리가 아니라 **기준**이다

문서는 작업이 끝난 뒤 쓰는 요약이 아니다. **작업의 근거이자 판정 기준**이다.

- **작업 전**: 무엇을 만들지 계획 문서에서 확인한다. 문서와 다르게 만들 이유가 있으면 **먼저 문서를 고친다.**
- **작업 중**: 문서에 없는 결정을 내렸으면 그 결정을 기록한다. 결정이 사라지면 다음 사람이 같은 고민을 반복한다.
- **작업 후**: **문서를 갱신한다.** 마일스톤 상태, 실제로 만든 모듈 · 경로, 바뀐 스키마, 폐기한 설계를 반영한다.

**코드와 문서가 어긋나면 둘 중 하나는 버그다.** 발견하면 어느 쪽이 옳은지 판단해 고친다.
어긋난 상태로 두지 않는다.

---

## 4. 테스트 규칙

### 4.1 기능을 추가하면 테스트도 함께 쓴다

예외 없다. **기능 코드와 테스트 코드는 같은 커밋에 들어간다.**

### 4.2 빌드 게이트

**빌드 시 모든 테스트가 통과해야 한다.** 하나라도 실패하면 병합하지 않는다.
**"로컬에선 됐다"는 근거가 아니다 — CI 가 판정한다.**

CI 가 판정하는 관문:

| 관문 | 도구 | 잡는 것 |
|---|---|---|
| 테스트 | <!-- PROJECT --> | 기능 회귀 |
| 린트 · 포맷 | <!-- PROJECT --> | 스타일 드리프트 |
| 타입 검사 | <!-- PROJECT --> | 계약 위반 |
| **명세 ↔ 코드 링크** | `tools/spec/check_spec_links.py` | **명세와 구현이 갈라진 것** (§2.4) |
| mermaid 문법 | <!-- PROJECT --> | 빈 상자로 렌더되는 다이어그램 (§2.3) |
| 생성물 미커밋 | `git diff --exit-code` | 손으로 고친 생성 코드 |
| **테스트 증거 규약** | <!-- PROJECT --> | 판정 없는 산출물 · 유출 위험 캡처 (§4.4) |
| **미룬 작업의 트리거** | <!-- PROJECT --> | 트리거가 발화했는데 아무도 모르는 것 (§4.5) |

> **프로젝트 고유 관문을 여기에 계속 추가한다.** 사람이 리뷰에서 반복해 지적하는 것이 있으면
> 그것은 관문 후보다 (§0).

### 4.3 회귀 테스트: 한 번 겪은 문제는 다시 겪지 않는다

작업 중 발생한 **문제 · 함정 · 삽질은 반드시 재현 테스트로 고정한다.** 순서를 지킨다.

```
1. 문제를 재현하는 테스트를 먼저 쓴다  → verify: 그 테스트가 실패하는 것을 확인
2. 원인을 고친다                       → verify: 그 테스트가 통과하는 것을 확인
3. 전체 테스트를 돌린다                 → verify: 다른 것이 깨지지 않았는지 확인
```

**테스트 없이 고친 버그는 반드시 다시 발생한다.**

재현 테스트를 못 쓰겠으면(타이밍 · 환경 의존 등) **왜 못 쓰는지와 어떻게 감지할지**를 문서에 남긴다.
플랫폼별 함정, 버전 특이사항, 환경별 편차처럼 코드로 못 막는 것은 설계 문서의 "함정" 절에 기록한다.

### 4.4 테스트는 증거를 남긴다

**화면 · 외부 시스템처럼 자동 단언만으로 확신할 수 없는 것을 검증하는 테스트는 산출물을 남긴다.**

```
.evidence/<branch>/<test-id>/
├── result.json      # 판정 · 지표 · 커밋 SHA · 실행 시각
├── shot-01.png      # 커밋한다 (회귀 비교용)
└── video.url        # 외부 스토리지 URL — 영상 자체는 커밋하지 않는다
```

이 규약이 있는 이유: **AI 는 화면을 볼 수 없고, 사람은 매 실행을 보지 않는다.**
그래서 "돌려 봤다"가 아니라 **판정 · 수치 · 프레임**이 파일로 남아야 완료다 (§0).

**세 가지는 예외 없다.**

1. **판정은 종료 코드로 나간다.** 로그를 사람이 읽어야만 아는 실패는 CI 가 잡지 못한다.
2. ⚠️ **캡처는 대상 화면만.** 에디터 UI · 터미널 · 소스가 프레임에 들어가면
   **스크린샷이 곧 소스 · 자격증명 유출**이다. 전체 데스크톱 캡처는 하네스에서 금지한다.
3. **영상 · 대용량 바이너리는 저장소에 넣지 않는다.** 외부 스토리지에 올리고 URL 만 남긴다
   (보존 기간과 개수 상한을 정해 둔다).

### 4.5 미룬 작업은 트리거로 등록하고, CI 가 발화를 감시한다

**"지금 하지 않는다"고 정한 것은 [`docs/triggers.json`](./docs/triggers.json) 에 조건과 함께 등록한다.**
검사기가 커밋된 지표로 각 트리거를 평가하고, **넘었는데 상태가 `대기` 면 실패한다.**

이 규칙이 있는 이유: 미룬 작업이 산문으로 흩어져 있으면 **작업자가 그 목록을 안 읽고
이미 등록된 계획을 다시 발명한다.** 근거까지 통째로 다시 유도하게 된다.

**관문이 강제하는 것은 "지금 당장 고쳐라"가 아니라 "넘은 사실을 인지하고 기록하라"** 다.
상태를 `발화` 로 바꾸면 통과한다 — 대신 그때부터 문서에 남는다. **"몰랐다"가 불가능해진다.**

⚠️ 지표가 없어 평가할 수 없으면 `측정불가` 로 두고 **왜 없는지를 적는다**
(§0 — 강제할 수 없으면 왜 못 하는지를 남긴다).

### 4.6 동시성 · 순환 의존처럼 "언젠가 멈추는" 구조는 정적으로 검사한다

**런타임에만 드러나고 재현이 어려운 실패는 테스트로 못 잡는다.** 정적 검사 관문으로 옮긴다.
전형적인 후보:

- **차단 호출만으로 이루어진 순환** — 액터 · 그레인 · 락 순서. 언젠가 반드시 데드락이 된다.
- **계층 위반** — 하위 레이어가 상위를 참조.
- **식별자 규약** — ID 중복 · 대역 위반 · 폐기 재사용.

⚠️ 이런 검사는 **"금지"가 아니라 "등록하라"로 만드는 편이 실용적이다.**
의도적으로 허용해야 하는 예외가 반드시 생기기 때문이다.
허용 목록은 파일로 두고, **예외 하나하나에 이유를 적는다.**

⚠️ **허용 단위를 너무 크게 잡으면 관문이 무력해진다.**
"이 모듈 쌍은 허용"으로 등록하면 그 쌍 사이에 위험한 간선이 하나 더 생겨도 통과한다.
**허용은 가장 작은 단위(간선 · 심볼)로 등록한다.**

---

## 5. 코딩 원칙

> 출처: [andrej-karpathy-skills / CLAUDE.md](https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md)
> — LLM 이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침.
>
> **트레이드오프**: 이 지침은 속도보다 신중함에 치우쳐 있다. 사소한 작업에는 판단해서 적용한다.

### 5.1 코딩 전에 생각한다

**추측하지 않는다. 혼란을 감추지 않는다. 트레이드오프를 드러낸다.**

구현 전에:

- **가정을 명시한다.** 불확실하면 묻는다.
- **해석이 여러 개면 모두 제시한다.** 조용히 하나를 고르지 않는다.
- **더 단순한 방법이 있으면 말한다.** 필요하면 반대 의견을 낸다.
- **불명확하면 멈춘다.** 무엇이 혼란스러운지 이름을 붙여 묻는다.

### 5.2 단순함 우선

**문제를 푸는 최소한의 코드. 추측성 코드는 쓰지 않는다.**

- 요청받지 않은 기능은 만들지 않는다.
- 한 번만 쓰는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성" · "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

스스로 물어본다: **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 단순화한다.

### 5.3 최소 범위 수정

**꼭 필요한 것만 건드린다. 자기가 만든 것만 치운다.**

기존 코드를 수정할 때:

- 옆에 있는 코드 · 주석 · 서식을 "개선"하지 않는다.
- 깨지지 않은 것을 리팩터링하지 않는다.
- 자기 방식이 더 낫다고 생각해도 **기존 스타일에 맞춘다.**
- 관련 없는 죽은 코드를 발견하면 **말만 한다** — 지우지 않는다.

내 변경이 고아를 만들었다면:

- **내 변경 때문에** 안 쓰이게 된 import · 변수 · 함수는 제거한다.
- 원래부터 죽어 있던 코드는 요청받지 않았으면 제거하지 않는다.

판정 기준: **바뀐 모든 줄이 사용자의 요청으로 직접 추적되는가.**

### 5.4 목표 기반 실행

**성공 기준을 정하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:

- "검증 추가" → "잘못된 입력에 대한 테스트를 쓰고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 쓰고, 통과시킨다"
- "X 리팩터링" → "전후로 테스트가 통과하는 것을 보장한다"

여러 단계 작업이면 짧게 계획을 밝힌다:

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준이 있으면 스스로 반복할 수 있다. 약한 기준("되게 만들어")은 계속 되묻게 만든다.

---

**이 지침이 작동하고 있다는 신호**: diff 에 불필요한 변경이 줄고, 과설계로 다시 쓰는 일이 줄고,
실수한 뒤가 아니라 **구현 전에** 질문이 나온다.

---

## 6. 폴더 규칙

| 폴더 | 담는 것 |
|---|---|
| `docs/spec/` | **명세** — "무엇을 · 왜"의 단일 진실 원천 (§2.4). 코드가 아니다 |
| `docs/` | 설계 문서 — "어떻게 · 함정" |
| `tools/` | 저장소 전체에 걸치는 도구 · 검사기. 제품 빌드에 포함되지 않는다 |
| `.github/` | CI 워크플로 |
| <!-- PROJECT --> | 컴포넌트 폴더를 여기에 추가한다 |

**제품 코드는 반드시 컴포넌트 폴더 안에 둔다. 루트에 코드를 두지 않는다.**

---

## 7. 프로젝트 고유 원칙

<!-- PROJECT -->

> 이 절은 프로젝트마다 다르다. 설계 문서에서 확립된 원칙을 여기에 요약하고, 상세는 문서로 링크한다.
> 각 항목은 **"무엇을 금지하고 무엇을 기본값으로 삼는가"** 로 쓴다. 취향이 아니라 판정 기준이어야 한다.
>
> 예시(지우고 다시 쓴다):
>
> 1. **신뢰 경계** — 어느 쪽을 권위로 두는가. 신뢰하지 않는 입력은 무엇인가.
> 2. **계약은 인터페이스 뒤에 숨긴다** — 상위 코드가 와이어 포맷 · SQL 을 직접 만들지 않는다.
> 3. **성능 제약은 설계 전제다** — "만들고 나서 최적화"가 아니라 "제약 안에서 설계".
> 4. **언어 · 프레임워크 코딩 표준** — 어느 표준을 따르는가. 링크.
